package com.pccp.자료형;

public class DataType {
    public static void main(String[] args) {
        // 정수(byte, short는 무시, int와 long만 기억해도 됨), 실수형 문자(float 무시, double만 기억해도 됨)
        int intNumber = 1;
        long longNumber = 10000000000L;
        double doubleNumber = 1000000000000.0;

        // 문자형과 문자열 생성 차이
        // 문자형의 생성: '' / 문자열의 생성: ""
        char charVariable = 'a';
        String StringVariable = "b";

        // 논리형(True, False)
        
        // 비교 연산자(2개의 값을 비교해서 True, False의 결과를 만드는 연산자,
        // 자바에서는 비교연산자 두 개 연속 불가 ex) 0 < a < 10 불가, 0 < a && a < 10 으로 해야됨.
        /* 같다(==), 다르다(!=) */
    }
}
